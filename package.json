{
  "license": "NONE",
  "name": "circuits-v2",
  "version": "2.0.0",
  "description": "ZK-SNARK circuits for RAILGUN",
  "scripts": {
    "prepare": "./scripts/fetch_circom",
    "prebuild": "./scripts/generate_circuits",
    "build": "./scripts/compile_circuits",
    "pretest": "./scripts/generate_circuits",
    "test": "PATH=./bin:$PATH mocha --exit --timeout 30000",
    "check": "./scripts/check_circuits"
  },
  "devDependencies": {
    "circom_tester": "0.0.18",
    "mocha": "^9.2.0"
  },
  "dependencies": {
    "circomlib": "2.0.5"
  }
}
